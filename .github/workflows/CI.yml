name: CI workflow

on:
  push:
    branches: main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      
      - name: download dependency
        uses: actions/setup-node@v3
        with:
          node-version: '14.x'
      


    
